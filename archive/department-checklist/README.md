# Archived Department Checklist

This folder contains the original 13-department checklist code and content.
It has been removed from the active product to make way for the L1/L2/L3 Line Check system, per client requirements.

To restore:
1. Move `checklist-content.ts` back to `src/lib/`
2. Move `staff-page.tsx` to `src/app/staff/page.tsx`
3. Move `staff-checklist-route.ts` to `src/app/api/staff/checklist/route.ts`
